В тази лекция ще научим как се създават проекти на C#. Ще разберем и значението на съкращението `IDE`.  Също така ще разберем какво представляват коментарите и как да отпечатваме съобщения в конзолата. Накрая ще конфигурираме опциите на `Visual Studio`.
Нека създадем първата си `C#` програма.
![[1.png]]
В този прозорец има няколко шаблона за проекти, от които да избираме.
![[2.png]]
Тези шаблони са за уеб, декстоп, игри и много други.
Както можем да видим, с `.NET` можем да създаваме програми за различни платформи и потребители. За този етап ние ще изберем `Console Application`, като се уверим, че сме избрали `C#`.
![[3.png]]
Нека да го наречем `TodoList`.
![[4.png]]
В този прозорец можем да изберем `.NET` версията.
![[5.png]]
Ние имаме `.NET 6`, но дори и да имаме разлч