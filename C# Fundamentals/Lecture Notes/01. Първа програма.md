В тази лекция ще научим как се създават проекти на C#. Ще разберем и значението на съкращението `IDE`.  Също така ще разберем какво представляват коментарите и как да отпечатваме съобщения в конзолата. Накрая ще конфигурираме опциите на `Visual Studio`.
Нека създадем първата си `C#` програма.
![[1.png]]
В този прозорец има няколко шаблона за проекти, от които да избираме.
![[2.png]]
Тези шаблони са за уеб, декстоп, игри и много други.
Както можем да видим, с `.NET` можем да създаваме програми за различни платформи и потребители. За този етап ние ще изберем `Console Application`, като се уверим, че сме избрали `C#`.
![[3.png]]
Нека да го наречем `TodoList`.
![[4.png]]
В този прозорец можем да изберем `.NET` версията.
![[5.png]]
Ние имаме `.NET 6`, но дори и да имаме различна версия, всичко отново ще работи нормално. 
И ето го нашия проект. Той е готов за работа още сега. Преди да го направим, нека да огледаме наоколо.
![[6.png]]

В този прозорец виждаме изходния код на нашата програма.

> [!Важно]
> Изходния код на програмата е просто текст.

Ние бихме могли да създаваме подобни програми и с `Notepad`, но това няма да е много удобно.

> [!Важно]
> Visual Studio е мощна `среда за разработка` - програма, която ни позволява да пишем изходен код.

Нека да модифицираме тази програма. 
На първо място, имаме коментар.
![[7.png]]

Това е текст, който не се компилира или изпълнява, затова можем да го премахнем.
Сега този ред.

![[8.png]]

Това е запис на ред текст в конзолата. Ние можем да променим съобщение, което ще се отпечата, на каквото пожелаем.

> [!code] Промяна на първоначалното съобщение
> ```csharp
>Console.WriteLine("Hello!");
> ```

В `C#` в края на всеки ред, ние трябва да завършим с точка и запетая.

![[9.png]]

Забравянето им е доста често срещана грешка от начинаещите.
Да видим другите прозорци във `Visual Studio`.

